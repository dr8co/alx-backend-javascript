# 0x02 - EcmaScript Classes

## Introduction

This project is about:

- How to use `class` and `extends` keywords
- How to use `super` and `this` keywords
- How to use `Object.getPrototypeOf`, `Object.getPrototypeOf` and `Object.create`
